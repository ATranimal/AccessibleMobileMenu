<input type="checkbox" name="preference" value="vegetarian">Vegetarian

<h1>Chez OO Menu</h1>
<ul>
    <% dishes.forEach(function(dish) { %>
        <li><i>$<%= dish.price%></i>  <%= dish.name %>: <%= dish.description%></li>
    <% }); %>
</ul>
